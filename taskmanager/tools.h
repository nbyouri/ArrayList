#include <errno.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

char *strjoin(const char *, char **);
int growArray(void *, size_t, size_t);
void dumpArray(char **, size_t);
